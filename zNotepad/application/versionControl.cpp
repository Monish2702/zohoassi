#include <iostream>
#include <fstream>
#include <string>
#include <regex>
#include <vector>
#include <iterator>
#include <filesystem>
#include "../notepad_proto/users.pb.h"
#include "../project_proto/projects.pb.h"
#include "../version_proto/versions.pb.h"

using namespace std;
//void version(notepad_projects::project p)
//{
  //  notepad_versions::version_list vl;
    //if(vl.versions(0).changes_count() == 0)
    //{
      // vl.set_project_id(p.project_id());
       //vl.versions(0).current_version(0).set_each_line(p.contents(0).content_line());
    //}
    //else
    //{
    //}
    //notepad_versions::version *v = vl.add_versions();
//}
